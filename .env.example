# ============================================================
# DIA Project — Variables d'environnement
# Copiez ce fichier en .env et renseignez les valeurs.
# ============================================================

# --- Base de données (obligatoire) ---
DATABASE_URL=mysql://user:password@localhost:3306/dia

# --- Authentification (obligatoire en production) ---
JWT_SECRET=changeme_use_a_long_random_string
OAUTH_SERVER_URL=https://your-oauth-server/
OWNER_OPEN_ID=your_admin_open_id

# --- Identifiant application Manus (obligatoire en production) ---
VITE_APP_ID=your_manus_app_id

# --- API Forge / LLM (obligatoire si les agents IA sont utilisés) ---
BUILT_IN_FORGE_API_URL=https://your-forge-api/
BUILT_IN_FORGE_API_KEY=your_forge_api_key

# --- Serveur ---
PORT=3000
NODE_ENV=development

# --- Analytics Umami (optionnel) ---
# Décommentez et renseignez si vous voulez activer les analytics Umami.
# Ajoutez ensuite le snippet dans client/index.html avant </body> :
#   <script defer src="${VITE_ANALYTICS_ENDPOINT}/umami"
#           data-website-id="${VITE_ANALYTICS_WEBSITE_ID}"></script>
# VITE_ANALYTICS_ENDPOINT=https://your-umami-instance.example.com
# VITE_ANALYTICS_WEBSITE_ID=your_website_uuid
